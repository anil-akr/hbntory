"""HBntory product MCP server.

Bridge between the AI system and the external Product API (read-only).
Exposes MCP tools that the AI agent calls to fetch product information instead
of inventing it.
"""
